import HeapLayout from '../components/Heap';

This is the second part of the series - [My Mental Model about Heap Structure](/posts/Heap-data-structure).

Below is a list of random numbers (read as an array of numbers) that we will be transforming to a heap structure.

<HeapLayout />
